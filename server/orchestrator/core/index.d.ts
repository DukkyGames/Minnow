/** Journal envelope version this build writes. */
export const CORE_VERSION: number;

export type {
  Role,
  AgentRole,
  AttemptResult,
  SeedKind,
  TaskPhase,
  BoardStatus,
  StopReason,
  EventEnvelope,
  JournalEvent,
  JournalEventType,
  ValidationResult,
  PlanTask,
  TaskGraph,
  ParseError,
  Attempt,
  TaskState,
  BoardState,
  Desired,
  Action,
  Snapshot,
} from './types';
