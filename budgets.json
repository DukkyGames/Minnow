{
  "version": 1,
  "rationale": "MIN-400 performance budgets. Ceilings guard against bundle and boot regressions in CI. Raise limits only deliberately in the same PR that needs the headroom — edit this file and refresh scripts/bundle-size-baseline.json after `npm run build`. eagerJsMaxKb sums the entry `<script type=\"module\">` plus every `rel=\"modulepreload\"` href in dist/index.html (cold-boot fetch set). Ratcheted 2026-08-20 after Vite 8 / dependency lock refresh pushed lazy store chunk and modulepreload graph past prior ceilings (3638 KB lazy, 4498 KB eager, 10194 KB assets on main). Ratcheted 2026-08-29 for MIN-635: main already sat ~3707 KB on the store chunk; Issues sparkles controls stay on that graph while the generations client + expand prompt/overlay load on first click (~3709 KB measured).",
  "bundle": {
    "entryJsMaxKb": 1500,
    "entryCssMaxKb": 950,
    "largestLazyJsMaxKb": 3720,
    "eagerJsMaxKb": 4600,
    "totalAssetsMaxKb": 10300
  },
  "startup": {
    "appReadyHarnessMaxMs": 2500
  }
}
