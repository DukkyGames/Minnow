{
  "version": 1,
  "rationale": "MIN-400 performance budgets. Ceilings guard against bundle and boot regressions in CI. Raise limits only deliberately in the same PR that needs the headroom — edit this file and refresh scripts/bundle-size-baseline.json after `npm run build`.",
  "bundle": {
    "entryJsMaxKb": 1500,
    "entryCssMaxKb": 950,
    "largestLazyJsMaxKb": 3200,
    "totalAssetsMaxKb": 9500
  },
  "startup": {
    "appReadyHarnessMaxMs": 2500
  }
}
