{
  "version": 1,
  "rationale": "MIN-400 performance budgets. Ceilings guard against bundle and boot regressions in CI. Raise limits only deliberately in the same PR that needs the headroom — edit this file and refresh scripts/bundle-size-baseline.json after `npm run build`. eagerJsMaxKb sums the entry `<script type=\"module\">` plus every `rel=\"modulepreload\"` href in dist/index.html (cold-boot fetch set). Ratcheted 2026-08-29 for MIN-647 queued-transcript / serve-activity-chips JS on the lazy store chunk (3709 KB measured; main already sat ~3707 KB over the prior 3700 KB ceiling).",
  "bundle": {
    "entryJsMaxKb": 1500,
    "entryCssMaxKb": 950,
    "largestLazyJsMaxKb": 3750,
    "eagerJsMaxKb": 4600,
    "totalAssetsMaxKb": 10300
  },
  "startup": {
    "appReadyHarnessMaxMs": 2500
  }
}
