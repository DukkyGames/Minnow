# v0.0.1 GitHub release setup

Plan for the first public beta on [HenriGrimm/Minnow](https://github.com/HenriGrimm/Minnow). Packaging and asset upload are **out of scope** until the release is built locally.

## Todos

- [x] Set `package.json` `version` to `0.0.1` (feeds `latest.yml` and in-app version).
- [x] Point `build.publish` at `HenriGrimm/Minnow` (must match the repo that hosts Releases for `electron-updater`).
- [x] Add user-facing release notes at [`documentation/releases/v0.0.1.md`](../releases/v0.0.1.md).
- [x] Create a **draft** GitHub release `v0.0.1` (pre-release, no installer assets yet). Edit: https://github.com/HenriGrimm/Minnow/releases (draft `v0.0.1 — Open beta`).
- [ ] Commit version + publish config + docs on `main` and push (tag target must include the `0.0.1` bump before publishing the release).
- [ ] Run [`release-e2e-testing.md`](../guides/release-e2e-testing.md) smoke checklist.
- [ ] `npm run package` → attach `Minnow-Setup-0.0.1.exe`, `latest.yml`, and `.blockmap` from `release/pkg/`.
- [ ] Publish the draft release (keeps **pre-release** checked for beta channel).
- [x] Align repo and sponsor links from `DukkyGames` to `HenriGrimm` (`HenriGrimm/Minnow`, GitHub Sponsors, FUNDING.yml).

## Reference

Maintainer flow: [`maintainer/releasing.md`](../maintainer/releasing.md).
