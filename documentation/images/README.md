# README screenshots

Drop captures here with these exact filenames and they appear in the README with no further edits.

| File | Shot | Used in |
|------|------|---------|
| `hero.png` | Minnow desktop: underwater wallpaper, dock, composer centered, menubar model chip visible. Wide 16:9. | README, top |

Guidelines: capture at 2× on a dark theme (`swamp-dark` is the default), crop to 16:9, keep it under ~600 KB, and use a real project rather than an empty state. PNG.

Extra shots for the guides are welcome — reference them from the guide that needs them and add a row above.
