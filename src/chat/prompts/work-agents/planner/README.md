# Planner

Default for **Plan** and **Super Plan**. File writes stay limited to plan markdown under `documentation/plans/`. The allowlist includes **`issue_*`** tools so the planner can search, file, update, and attach plans to Issues without implementing code.
