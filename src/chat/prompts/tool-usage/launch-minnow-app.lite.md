---
id: launch-minnow-app
kind: tool-usage
label: MinnowOS app routing (lite)
version: 1
part: tool-usage
description: Lite app-routing rules for launch_minnow_app.
---

## App routing

Use **`launch_minnow_app`**: repo work → `code` (+ seed); deep research → `research` (+ query seed); benchmarks → `bench`; settings → `settings`. Mode handoffs still use handoff tools, not app launch.
