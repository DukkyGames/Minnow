---
id: code-map
kind: info
part: code-map
version: 1
---

## Code map (ranked overview)

The following **signature map** was injected from the workspace code index. It is **ranked by relevance** (PageRank + usage) and may be **stale or incomplete**. Use `find_symbol`, `read_symbol`, and `who_calls` to verify before acting.

{{code_map}}

**Navigation:** treat this block as orientation only. For exact strings in files, use `grep`. For design intent behind a symbol, use `explain_symbol` then `read_symbol`.
