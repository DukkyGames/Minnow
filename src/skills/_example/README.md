# Skill author template

Copy this folder to `~/.minnow/skills/<your-skill-id>/` and edit `SKILL.md`.

- Front matter `name` must match the directory name.
- Restart is not required; refresh the app or call `GET /api/skills` after adding files.
- Built-in skills in `src/skills/` are overridden by user skills with the same id.
