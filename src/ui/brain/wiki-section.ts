/**
 * Brain app — Wiki section re-exports (graph home absorbed tree + viewer).
 */

export {
  navigateBrainWikiPage,
  navigateBrainGraphPage,
  getWikiSelectedPath,
  setWikiSelectedPath,
  getGraphSelectedPath,
  setGraphSelectedPath,
  renderWikiSection,
  renderGraphSection,
  setGraphOrphanPaths,
} from './graph-section';
